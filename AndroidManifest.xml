<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.diary.crazy"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="10"
        android:targetSdkVersion="10" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
       <activity
            android:name="com.diary.crazy.CrazyDemos"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- UI begin here -->
        <activity
            android:name=".chapter02.CodeViewActivity"
            android:label="@string/codeViewActivity" >
            <intent-filter>
                <action android:name="android.intent.diary.Action" />

                <category android:name="android.intent.category.SAMPLE_CODE" />
            </intent-filter>
        </activity>
               <activity
            android:name=".chapter02.CustomViewActivity"
            android:label="@string/customViewActivity" >
            <intent-filter>
                <action android:name="android.intent.diary.Action" />

                <category android:name="android.intent.category.SAMPLE_CODE" />
            </intent-filter>
        </activity>
                     <activity
            android:name=".chapter02.MixViewActivity"
            android:label="@string/mixViewActivity" >
            <intent-filter>
                <action android:name="android.intent.diary.Action" />

                <category android:name="android.intent.category.SAMPLE_CODE" />
            </intent-filter>
        </activity>
        
        
    </application>

</manifest>
